# https://docs.codecov.com/docs/codecov-yaml
coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        informational: true

comment:
  layout: "reach,diff,flags,files"
  behavior: default
